#!/bin/bash
# Script: data_extraction_automation-0x01
# Purpose: Extract Pokémon name, height, weight, and type from data.json

JSON_FILE="data.json"

# Extract raw values
name=$(jq -r '.name' "$JSON_FILE" | sed 's/.*/\u&/')
height=$(jq -r '.height' "$JSON_FILE")
weight=$(jq -r '.weight' "$JSON_FILE")
type=$(jq -r '.types[0].type.name' "$JSON_FILE" | sed 's/.*/\u&/')

# Convert units (decimeters -> meters, hectograms -> kilograms)
formatted_height=$(awk -v h="$height" 'BEGIN { printf "%.1f", h / 10 }')
formatted_weight=$(awk -v w="$weight" 'BEGIN { printf "%.0f", w / 10 }')

# Output in the required strict format
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
